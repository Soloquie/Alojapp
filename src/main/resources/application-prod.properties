# Production Configuration - PostgreSQL
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# Production optimizations
spring.datasource.hikari.maximum-pool-size=5
server.port=${PORT:8080}

# Security
app.jwt.secret=${JWT_SECRET:super-secreto-muy-largo-min-32-chars-para-HS256-por-ejemplo-1234567890}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:3600000}

# Cloudinary
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dw8rfhzon}
cloudinary.api-key=${CLOUDINARY_API_KEY:344278657232165}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:gbkf_Ukg61s1hP2rUEbQgXKQMZg}

# File upload
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=alojappprogra@gmail.com
spring.mail.password=mlih bnnb cizz yruj
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.default-encoding=UTF-8
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

app.mail.from=alojappprogra@gmail.com
app.mail.from-name=Alojapp
app.frontend.reset-url=${FRONTEND_URL:http://localhost:5173}/reset-password
app.recuperacion.max-codigos-activos=3
app.recuperacion.cooldown-segundos=60

# CORS
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:4200,https://tu-app.netlify.app}

# Production logging
logging.level.root=INFO
logging.level.co.uniquindio=INFO
logging.level.org.springframework=WARN
logging.level.org.hibernate=WARN

# Disable Swagger in production
springdoc.swagger-ui.enabled=false
springdoc.api-docs.enabled=false